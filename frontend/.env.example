# Public browser URLs only. Leave both unset for same-origin /api and /auth.
# When set, both URLs must share one origin. HTTPS pages require HTTPS endpoints;
# http://localhost endpoints are intended only for a page itself served over HTTP.
VITE_API_BASE_URL=
VITE_AUTH_BASE_URL=
